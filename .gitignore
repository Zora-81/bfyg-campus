# 运行时生成 / 数据库（SQLite WAL 模式）
server/data.db
server/data.db-journal
server/data.db-wal
server/data.db-shm
server/data.db.bak
server/uploads/*
!server/uploads/.gitkeep

# 服务日志
server/__server.log
server/server_stdout.log
server/server_stderr.log

# 依赖
node_modules/
server/node_modules/

# 环境变量与密钥
.env
.env.local
.env.*.local

# Cloudflare Wrangler 本地状态
.wrangler/
worker/.wrangler/

# 构建产物（当前 web_build 由 _build.mjs 生成，可重建）
web_build/

# Windows 误生成的空设备文件
nul

# 工作区
.workbuddy/

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

# 本地生成 / 工作产物（不入库）
campus-promo/            # Remotion 宣传视频生成器（源码本地保留，不公开）
video/capture/           # 视频抓帧工作目录
*.log
.DS_Store
Thumbs.db
desktop.ini
