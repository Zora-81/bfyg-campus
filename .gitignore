# 运行时生成 / 数据库（SQLite WAL 模式）
server/data.db
server/data.db-journal
server/data.db-wal
server/data.db-shm
server/data.db.bak
server/uploads/*
!server/uploads/.gitkeep

# 服务日志
server/__server.log
server/server_stdout.log
server/server_stderr.log

# 依赖
node_modules/
server/node_modules/

# 环境变量与密钥
.env
.env.local
.env.*.local

# Cloudflare Wrangler 本地状态
.wrangler/
worker/.wrangler/
_worker.bundle

# 构建产物（当前 web_build 由 _build.mjs 生成，可重建）
web_build/

# Windows 误生成的空设备文件
nul

# 工作区
.workbuddy/

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

# 本地生成 / 工作产物（不入库）
# Remotion 宣传视频生成器（源码本地保留，不公开）
campus-promo/
*.log
.DS_Store
Thumbs.db
desktop.ini

# 工作区回收站（根目录散落截图/临时文件统一收容）
_trash/

# 独立子项目（自带 .git，不纳入主仓库）
campus-3d-showcase/

# 临时对比/在线调试产物
_live_style.css
prod_idx.html
prod_intro.css
prod_css.txt
html/index.html.bak
